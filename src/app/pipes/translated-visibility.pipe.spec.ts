import { TranslatedVisibilityPipe } from './translated-visibility.pipe';

describe('TranslatedVisibilityPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatedVisibilityPipe();
    expect(pipe).toBeTruthy();
  });
});
